{
  "//": "Copy to user_credentials.json and set real passwords. user_credentials.json is gitignored.",
  "root_enc_password": "changeme123",
  "users": [
    {
      "username": "admin",
      "!password": "changeme123",
      "sudo": true
    }
  ]
}
